// Neste desafio, foi usado indexação da array para acessar itens dentro de uma array. Indexação da array começa em 0. Por exemplo, myArray[0] vincula-se ao 1º item na array denominada myArray. Enquanto myArray[1] se vincula ao 2º item.

var colors = [
    'black',
    'white',
    ___
  ];
  drawBox(colors[0]); // busca o valor 'black'
  drawBox(colors[1]); // busca o valor 'white'
  drawBox(colors[0]);
  newLine();
  drawBox(colors[1]);
  drawBox(colors[0]);
  drawBox(colors[1]);
  newLine();
  drawBox(colors[0]);
  drawBox(colors[1]);
  drawBox(colors[0]);